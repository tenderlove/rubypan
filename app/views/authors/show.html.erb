<h1><%= @author.name || 'nil name' %></h1>

<%= render(
  :partial => 'releases/list',
  :locals => {
  :releases => @author.releases.select { |r| r.latest? }.sort_by { |release|
      release.ruby_gem.name
    }
  }
) %>
